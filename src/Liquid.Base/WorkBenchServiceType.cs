namespace Liquid
{
    public enum WorkBenchServiceType { Repository, Telemetry, Worker, MediaStorage, Cache, EventHandler, Logger };
    public enum WorkBenchServiceHttp { GET, POST, PUT, OPTIONS, DELETE, HEAD, TRACE, CONNECT, PATH }
}
